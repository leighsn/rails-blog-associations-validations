<p id="notice"><%= notice %></p>

<p>
  <h3>Name:</h3>
  <%= @post.name %>
  <% @post.tags.each do |p_t| %>
    <h4>Tags: </h4>
    <h5><%=p_t.name %> </h5>
    <% end %>
</p>


<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
